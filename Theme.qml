import QtQuick 6.5

QtObject {
    id: theme

    // property color bodyColor: isDark ? "#121212" : "#ffffff"
    // property color systemColor: isDark ? "" : ""
    // property color textColor: isDark ? "#121212" : "#E9EEF6"
    // property color borderColor: isDark ? "" : ""

    // === 阴影统一样式 ===



}
